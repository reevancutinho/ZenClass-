1) compare two JSON have the same properties without order

var obj1 = { name: "Person 1", age:5 };
var obj2 = { age:5, name: "Person 1" };

var flag=true;

for(key in obj1){ 
    if(obj1[key] == obj2[key]){ //comparing values based on key i.e name & age
        continue; 
    }else{
        flag=false;
        break;
    }
}

console.log(flag)


OUTPUT:
PS C:\Users\chris\desktop\b27\DOM> node script.js
true
